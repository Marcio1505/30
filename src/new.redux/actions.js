// TO DO Change import of application Actions to redux default folter to exclude

import applicationActions from './application/application.actions';

export { applicationActions };

export default {
  applicationActions,
  application: applicationActions,
};
